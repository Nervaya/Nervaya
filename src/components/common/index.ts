/**
 * Shared UI components. Use these across pages for consistency.
 * Button: primary/secondary variants, loading state.
 * Input: form input with label, error state.
 * Loader: loading spinner, optional text.
 * QuantitySelector: min/max quantity with +/- buttons.
 */
export { default as Button } from './Button';
export { default as Input } from './Input';
export { default as Loader } from './Loader';
export { default as QuantitySelector } from './QuantitySelector';
